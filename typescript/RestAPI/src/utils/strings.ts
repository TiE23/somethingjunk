/**
 * Simple string utility function converts a string into UPPERCASE.
 * @param value the string we want make UPPERCASE
 * @returns given value made UPPERCASE
 */
export function toUpperCase(value: string) {
  return value.toUpperCase();
}
